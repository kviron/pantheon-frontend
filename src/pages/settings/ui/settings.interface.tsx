import { Typography } from 'antd'

export const SettingsPageInterface = () => {
    return (
        <div>
            <Typography.Title level={4}>{'Интерфейс'}</Typography.Title>
        </div>
    )
}
